# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# service-account keys — never commit
sa.json
*-firebase-adminsdk-*.json
service-account*.json

# debug
dev.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example
# Firebase web config is public (NEXT_PUBLIC_*) — committed so hosts build from git
!.env.production

# typescript
*.tsbuildinfo
next-env.d.ts
